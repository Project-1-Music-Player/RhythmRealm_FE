const routes = {
    home: '/',
    login: '/login',
    register: '/register',
    search: '/search',
    upload: '/upload',
    playlist: '/playlist/:id',
    profile: '/profile/:id',   
    song: '/song/:id',
}

export default routes